const baseURl = 'http://todo-rest-api.test/api/';

//Auth
export const authenticate = `${baseURl}authenticate`;
export const authenticateByToken = `${baseURl}authenticateByToken`;
export const logout = `${baseURl}logout`;

//Tasks
export const tasks = `${baseURl}tasks`;
